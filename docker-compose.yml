version: '3'

services:
  ducho:
    image: sisinflabpoliba/ducho
    tty: true
    stdin_open: true
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    volumes:
      - ./data:/Ducho/data
